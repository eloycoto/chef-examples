# memcached default config file
# 2003 - Jay Bonci <jaybonci@debian.org>
# This configuration file is read by the start-memcached script provided by
# the memcached package.

# Run memcached as a daemon
-d

# Log memcached's output to /var/log/memcached
logfile /var/log/memcached.log

# Start with a cap of 64 megs of memory
-m {{ memcached_memory }}

# Default connection port is 11211
-p {{ memcached_port }}

# Run the daemon as memcache:memcache
-u {{ memcached_user }}

# Specify which IP address to listen on. Default is to listen on all IP addresses
# This parameter is one of the only security measures that memcached has, so make sure
# it's listening on a firewalled interface.
-l {{ memcached_listen }}

# Limit the number of simultaneous incoming connections
-c {{ memcached_maxconn }}

# Use a larger memory slab size
-I {{ memcached_item_size }}

# Enable UDP
-U {{ memcached_port }}