# 1f7adb1e-2973-4b13-9f1a-eb750a613436.myprj

Ansible project generated by x2a-convertor.

## Roles


### cache


Configures caching services (memcached and redis)


**Target platforms:** Ubuntu, EL


Run this role:

```bash
ansible-playbook playbooks/run_cache.yml -i inventory/hosts.yml
```


**Default variables** (override in inventory or extra vars):

| Variable | Default |
|----------|---------|

| `memcached_port` | `11211` |

| `memcached_user` | `memcache` |

| `memcached_max_connections` | `1024` |

| `memcached_memory` | `64` |

| `memcached_listen_address` | `127.0.0.1` |

| `memcached_extra_options` | `` |

| `redis_user` | `redis` |

| `redis_group` | `redis` |

| `redis_servers` | `[{'port': 6379, 'bind': '127.0.0.1', 'unixsocket': '', 'unixsocketperm': 755, 'timeout': 0, 'loglevel': 'notice', 'logfile': '/var/log/redis/redis.log', 'databases': 16, 'save': ['900 1', '300 10', '60 10000'], 'rdbcompression': 'yes', 'dbfilename': 'dump.rdb', 'dir': '/var/lib/redis', 'slaveof': '', 'masterauth': '', 'maxclients': 10000, 'maxmemory': '0', 'maxmemory_policy': 'volatile-lru', 'appendonly': 'no', 'appendfsync': 'everysec', 'no_appendfsync_on_rewrite': 'no', 'auto_aof_rewrite_percentage': 100, 'auto_aof_rewrite_min_size': '64mb', 'requirepass': ''}]` |




### fastapi_tutorial


Configures FastAPI tutorial application with PostgreSQL


**Target platforms:** Ubuntu, EL


Run this role:

```bash
ansible-playbook playbooks/run_fastapi_tutorial.yml -i inventory/hosts.yml
```


**Default variables** (override in inventory or extra vars):

| Variable | Default |
|----------|---------|

| `fastapi_app_path` | `/opt/fastapi-tutorial` |

| `fastapi_project_name` | `FastAPI Tutorial` |

| `fastapi_api_version` | `1.0.0` |

| `fastapi_host` | `0.0.0.0` |

| `fastapi_port` | `8000` |

| `fastapi_service_user` | `root` |

| `fastapi_repo_url` | `https://github.com/dibanez/fastapi_tutorial.git` |

| `fastapi_repo_version` | `main` |

| `fastapi_db_host` | `localhost` |

| `fastapi_db_name` | `fastapi_db` |

| `fastapi_db_user` | `fastapi` |

| `fastapi_db_password` | `fastapi_password` |

| `fastapi_system_packages` | `['python3', 'python3-pip', 'python3-venv', 'git', 'postgresql', 'postgresql-contrib', 'libpq-dev']` |




### nginx_multisite


Configures a secure Nginx web server with multiple virtual hosts, each with SSL enabled


**Target platforms:** Ubuntu


Run this role:

```bash
ansible-playbook playbooks/run_nginx_multisite.yml -i inventory/hosts.yml
```


**Default variables** (override in inventory or extra vars):

| Variable | Default |
|----------|---------|

| `nginx` | `{'sites': {'test.cluster.local': {'document_root': '/opt/server/test', 'ssl_enabled': True}, 'ci.cluster.local': {'document_root': '/opt/server/ci', 'ssl_enabled': True}, 'status.cluster.local': {'document_root': '/opt/server/status', 'ssl_enabled': True}}, 'ssl': {'certificate_path': '/etc/ssl/certs', 'private_key_path': '/etc/ssl/private'}}` |

| `security` | `{'fail2ban': {'enabled': True}, 'ufw': {'enabled': True}, 'ssh': {'disable_root': True, 'password_auth': False}}` |




## Prerequisites

### Collections

Install required Ansible collections:

```bash
ansible-galaxy collection install -r collections/requirements.yml
```


## Inventory

Edit `inventory/hosts.yml` to define your target hosts before running any playbook.

## Quick start

```bash
# Install required collections
ansible-galaxy collection install -r collections/requirements.yml

# Edit inventory
vi inventory/hosts.yml

# Run a playbook
ansible-playbook playbooks/run_<role_name>.yml -i inventory/hosts.yml
```