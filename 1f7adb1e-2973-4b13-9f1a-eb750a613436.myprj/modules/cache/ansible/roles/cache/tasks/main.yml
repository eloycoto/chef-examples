---
- name: Include memcached tasks
  ansible.builtin.import_tasks: memcached.yml
- name: Create Redis log directory
  ansible.builtin.file:
    path: /var/log/redis
    state: directory
    owner: redis
    group: redis
    mode: "0755"
    recurse: true
- name: Include Redis tasks
  ansible.builtin.import_tasks: redis.yml
- name: Fix Redis configuration
  ansible.builtin.lineinfile:
    path: /etc/redis/6379.conf
    regexp: "{{ item }}"
    state: absent
  loop:
    - ^replica-serve-stale-data.*$
    - ^replica-read-only.*$
    - ^repl-ping-replica-period.*$
    - ^client-output-buffer-limit.*$
    - ^replica-priority.*$
  when: ansible_service_mgr == 'systemd'
- name: Include Redis enable tasks
  ansible.builtin.import_tasks: redis_enable.yml
