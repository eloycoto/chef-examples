---
- name: Restart memcached
  ansible.builtin.service:
    name: memcached
    state: restarted
- name: Restart redis
  ansible.builtin.service:
    name: "{{ 'redis@' + redis_port | string if ansible_service_mgr == 'systemd' else 'redis' + redis_port | string }}"
    state: restarted
